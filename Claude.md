# Claude Instructions

Use `skill.md` as the single source of truth for Staynex project standards.

Before making product, design, architecture, or code decisions:

1. Read `skill.md`.
2. Follow its Agent Routing section.
3. Use `Staynex_plan.md`, `Staynex_Blueprint.pdf`, and `docs/architecture.md` only when the task needs deeper context.
4. Treat `staynex-prototype/` as reference unless the user explicitly asks to modify it.

Do not duplicate project rules in this file. Update `skill.md` instead.
